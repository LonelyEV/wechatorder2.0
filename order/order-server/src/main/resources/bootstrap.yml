spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVICE
      profile: test
  redis:
    host: 192.168.21.244
    port: 6379
#    stream:
#      binders:
#        myMessage:
#          group: order
#          content-type: application/json

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false



